# graylog_cluster
Graylog Cluster, load balanced via nginx, with and without SSL certs

#This is a test example, will be updated as configurations are verified.

# Each folder can be extracted to run docker-compose.yml in folder/directory of choice. Creation of a .env file could be utilized if passwords within docker-compose.yml are a concern.

# The nginx.conf file will need to be placed in the same directory as the docker compose file.
